module.exports = {
    // 箭头函数中的括号
    arrowParens: 'always',
    // 对象括号中的空格
    bracketSpacing: true,
    // 行末尾标识
    endOfLine: 'auto',
    // 编辑器每行的长度，默认80
    printWidth: 90,
    // 制表符tab的宽度，默认值是2
    tabWidth: 4,
    // 是否使用单引号，默认为false，不适用单引号，使用双引号
    singleQuote: true,
    // 末尾逗号 none 末尾没有逗号，es5 有效的地方保留，all 在可能的地方都加上逗号
    trailingComma: 'none',
    // 忽略格式化html空白
    HTMLWhitespaceSensitivity: 'ignore',
    // 句末是否要分号
    semi: false,
    // Semicolons: false
}
